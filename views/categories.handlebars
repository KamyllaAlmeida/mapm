{{> map}}

<h4>List of points</h4>
<ul class="list-group">
  {{#each point_data}}
    <li id="{{this.id}}" class="list-group-item d-flex justify-content-between align-items-center">
      {{this.title}} | {{this.lat}} - {{this.long}}
    </li>
  {{/each}}
</ul>
